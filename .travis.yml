language: node_js
node_js:
  - 12
  

  

env:
  - DATABASE_URL=postgresql://postgres@localhost:5432/mygreets

node_js:  
  - "8.9.4"

before_script:
  - psql -c 'create database  mygreets;' -U postgres
  - psql -U postgres -d mygreets -a -f mygreet.sql

services:
  - postgresql
  